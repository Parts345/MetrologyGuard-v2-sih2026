import { ShieldCheck, LockKeyhole, ArrowRight } from "lucide-react";
import { useNavigate } from "react-router-dom";
export function LoginPage() { const navigate = useNavigate(); return <main className="login-page"><section className="login-brand"><div className="login-brand-inner"><span className="brand-mark"><ShieldCheck size={27} /></span><p className="eyebrow">Regulatory inspection system</p><h1>MetrologyGuard</h1><p>Legal Metrology Compliance Platform</p><div className="login-boundary"><LockKeyhole size={18} /><span>Secure, evidence-oriented packaged commodity assessment for authorised compliance teams.</span></div></div></section><section className="login-form-area"><form className="login-card" onSubmit={(event) => { event.preventDefault(); navigate("/dashboard"); }}><div><h2>Sign in</h2><p>Use your authorised organisation account to continue.</p></div><label>Work email<input type="email" required placeholder="officer@organisation.gov.in" autoComplete="email" /></label><label>Password<input type="password" required placeholder="Enter your password" autoComplete="current-password" /></label><button className="button primary login-submit" type="submit">Continue to platform<ArrowRight size={17} /></button><small className="login-footnote">Production deployments connect this screen to the configured identity provider. No credentials are stored in this client.</small></form></section></main>; }
