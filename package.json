{
  "name": "metrologyguard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@metrologyguard/api & npm run dev --workspace=@metrologyguard/web",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
  }
}
